{
  "name": "backend",
  "version": "1.0.0",
  "dependencies": {
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "msnodesqlv8": "^5.1.1"
  },
  "scripts": {
    "start": "node index.js",
    "build-client": "cd ../sanchar6t-replica-site && npm install && npm run build && cp -r build ../backend/public"
  }
}
